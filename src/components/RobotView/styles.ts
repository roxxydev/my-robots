import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {
    flex: 1,
    marginVertical: 10,
    alignItems: 'center',
  },
  image: {
    alignItems: 'center',
  },
  text: {
    textAlign: 'center',
  },
});

export default styles;
