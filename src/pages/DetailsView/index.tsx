import * as React from 'react';
import Container from '../../components/Container';
import RobotView from '../../components/RobotView';

const DetailsView = (): JSX.Element => {
  return (
    <Container>
      <RobotView />
    </Container>
  );
};

export default DetailsView;
